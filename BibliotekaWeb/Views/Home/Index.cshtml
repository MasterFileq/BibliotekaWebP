@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Witaj w aplikacji BibliotekaWeb</h1>
    <p class="lead">Zarządzaj swoją biblioteką w prosty i intuicyjny sposób</p>
</div>

<div class="alert alert-info" role="alert">
    <h4 class="alert-heading">Informacje o preinstalowanych użytkownikach:</h4>
    <p>Aby ułatwić konfiguracje, przygotowane są kont użytkowników z różnymi rolami:</p>
    <ul>
        <li><strong>Administrator:</strong> Login: <code>a@b.c</code></li>
        <li><strong>Bibliotekarz:</strong> Login: <code>b@c.d</code></li>
        <li><strong>Czytelnik:</strong> Login: <code>c@d.e</code></li>
    </ul>
    <p>Wszystkie konta mają wspólne hasło: <strong>Aa123456!</strong></p>
    <hr>
    <p class="mb-0">Zaloguj się na konto administratora, aby skonfigurować aplikacje. Pamiętaj, aby zmienić dane logowania.</p>

</div>

<div class="row">
    @if (User.IsInRole("Administrator") || User.IsInRole("Bibliotekarz") || User.IsInRole("Czytelnik"))
    {
        <div class="col-md-4">
            <div class="card mb-4 box-shadow">
                <div class="card-body">
                    <h5 class="card-title">Katalog książek</h5>
                    <p class="card-text">Przeglądaj książki w prosty i przystępny sposób.</p>
                    <a href="Ksiazkas/Katalog" class="btn btn-primary">Przeglądaj książki</a>
                </div>
            </div>
        </div>
    }

    @if (User.IsInRole("Czytelnik"))
    {
        <div class="col-md-4">
            <div class="card mb-4 box-shadow">
                <div class="card-body">
                    <h5 class="card-title">Wypożyczone książki</h5>
                    <p class="card-text">Sprawdź swoje wypożyczone książki i zarządzaj nimi.</p>
                    <a href="Ksiazkas/Wypozyczone" class="btn btn-primary">Moje wypożyczenia</a>
                </div>
            </div>
        </div>
    }

    @if (User.IsInRole("Administrator") || User.IsInRole("Bibliotekarz"))
    {
        <div class="col-md-4">
            <div class="card mb-4 box-shadow">
                <div class="card-body">
                    <h5 class="card-title">Zarządzanie czytelnikami</h5>
                    <p class="card-text">Zarządzaj kontami czytelników.</p>
                    <a href="Czytelniks/Index" class="btn btn-primary">Przeglądaj czytelników</a>
                </div>
            </div>
        </div>
    }

    @if (User.IsInRole("Administrator") || User.IsInRole("Bibliotekarz"))
    {
        <div class="col-md-4">
            <div class="card mb-4 box-shadow">
                <div class="card-body">
                    <h5 class="card-title">Zarządzanie wypożyczeniami</h5>
                    <p class="card-text">Monitoruj i zarządzaj wypożyczeniami książek. Śledź terminy zwrotów.</p>
                    <a href="Bibliotekarz/Index" class="btn btn-primary">Przeglądaj wypożyczenia</a>
                </div>
            </div>
        </div>
    }
</div>